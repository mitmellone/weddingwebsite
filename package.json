{
  "private": true,
  "name": "weddingwebsite",
  "devDependencies": {
    "chalk": "^2.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "yarn workspace weddingwebsite-frontend format",
    "start-frontend": "yarn workspace weddingwebsite-frontend start",
    "start-backend": "yarn workspace weddingwebsite-backend start:watch"
  }
}
